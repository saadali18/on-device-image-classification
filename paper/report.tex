\documentclass{article}
% Recommended, but optional, packages for figures and better typesetting:
\usepackage{amsmath}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{subfigure}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage[accepted]{icml2025}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{amsthm}
\usepackage{xurl}
\usepackage[capitalize,noabbrev]{cleveref}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% THEOREMS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\usepackage[textsize=tiny]{todonotes}

\begin{document}
\icmltitlerunning{\icmltitlerunning{Knowledge Distillation for Efficient On-Device Image Classification}}
\twocolumn[
\icmltitle{Knowledge Distillation for Efficient On-Device Image Classification}

\icmlsetsymbol{equal}{*}

\begin{icmlauthorlist}
\icmlauthor{Abdul Hafeez (25280098)}{lums}
\icmlauthor{Verda Batool (24280065)}{lums}
\icmlauthor{Saad Ali (25280011)}{lums}
\end{icmlauthorlist}

\icmlaffiliation{lums}{Department of Electrical Engineering, Lahore University of Management Sciences, Lahore, Pakistan}

\icmlcorrespondingauthor{Saad Ali}{25280011@lums.edu.pk}

\icmlkeywords{Knowledge Distillation, Model Compression, On-Device Deep Learning, CIFAR-100, ResNet, Temperature Scaling, Ablation Study}
\vskip 0.3in
]

\noindent\textbf{Code:} \url{https://github.com/saadali18/on-device-image-classification}\\


% ========================================================================
\begin{abstract}
Deploying accurate image classifiers on resource-constrained edge devices requires compact models that preserve most of a large model's accuracy. We study response-based knowledge distillation \citep{hinton2015distilling} as a principled compression strategy on CIFAR-100, using a pretrained ResNet-110 teacher ($73.48\%$ Test Top-1) to supervise a ResNet-20 student ($6.3\times$ parameter reduction) via a combined hard-label cross-entropy and temperature-scaled KL divergence objective. Against a hard-label ResNet-20 baseline of $67.63\%$ Test Top-1, our temperature sweep $\tau \in \{2, 4, 8, 16\}$ and loss-weighting sweep $\alpha \in \{0.1, 0.5, 0.9\}$ both exhibit clean inverted-U behaviour with peaks at $\tau^* = 8$ and $\alpha^* = 0.5$, where the distilled student reaches $\mathbf{69.98\%}$ Top-1 ($+\mathbf{2.35}$ pp, recovering $\mathbf{40\%}$ of the $5.85$ pp teacher--baseline gap). We extend the study with an architectural-scaling sweep across student depth (ResNet-$\{20, 32, 44, 56\}$) and width ($w \in \{0.5, 1, 2\}$) \textit{[depth/width headline numbers to be inserted after Step 3]}. A distributional analysis of the teacher's softened logits provides direct visual evidence of the dark knowledge transferred to the student, while a calibration analysis reveals a non-obvious side-effect: distillation degrades calibration (ECE rises from $6.23\%$ to $10.32\%$) as the student inherits the teacher's overconfidence pattern.
\end{abstract}
% ========================================================================

\section{Introduction}
\label{sec:intro}
Deep Convolutional Neural Networks (CNNs) like ResNet-110 achieve state-of-the-art accuracy using skip connections in an over-parameterized regime ($\approx$1.7 million parameters) \citep{he2016deep}. However, the lifecycle of such a deep learning model is characterized by a fundamental asymmetry between the training and deployment phases. During the \textit{training} phase, computational resources are typically abundant: parallelized GPU clusters, large batch sizes, extensive epochs, and regularizers all serve the primary objective of maximizing generalization over a held-out test set. Latency is not a concern. Conversely, during \textit{deployment} to edge devices (e.g., mobile phones, embedded microcontrollers), the constraints flip entirely. The memory becomes strictly limited, making models like ResNet-110 impractical for such resource-constrained environments. Additionally, such hardware often lacks the massive parallel throughput of a GPU, enforcing hard limits on inference latency.
% To formalize this using the standard notation for a feedforward neural network, let the input to a layer $l$ be denoted as $\vec{x}^{(l-1)} \in \mathbb{R}^{M^{(l-1)}}$, where $M^{(l-1)}$ is the dimensionality of the previous layer. The layer is parameterized by a weight matrix $W^{(l)} \in \mathbb{R}^{M^{(l)} \times M^{(l-1)}}$ and a bias vector $\vec{b}^{(l)} \in \mathbb{R}^{M^{(l)}}$. The forward pass computes the pre-activation $\vec{a}^{(l)} = W^{(l)}\vec{x}^{(l-1)} + \vec{b}^{(l)}$ and applies a non-linear activation function $\vec{x}^{(l)} = g(\vec{a}^{(l)})$.

\citet{compression2006} first introduced the idea of model compression, demonstrating that a compact, faster neural network (student model) can be trained to approximate the function of a much larger, slower model (teacher model) with minimal loss in accuracy. \citet{hinton2015distilling} formalized this as \emph{Knowledge Distillation} (KD) for neural networks. % The key question then is: how much accuracy survives the move to a much smaller network?

In standard $K$-class classification, a network produces $K$ logits and passes them through the softmax function
\begin{equation}
  \hat{y}_k = P(y = k \mid \vec{x})
             = \frac{e^{z_k}}{\sum_{j=1}^K e^{z_j}},
  \label{eq:softmax}
\end{equation}
where $z_k$ are the pre-softmax logits. Softmax translates unbounded logits into a normalized probability distribution which is optimized via the categorical cross-entropy loss
\begin{equation}
    L = -\frac{1}{N}\sum_{i=1}^N \sum_{k=1}^K y_{i,k} \log(\hat{y}_{i,k})
    \label{eq:cce}
\end{equation}
against one-hot hard labels for the ground-truth class $\vec{y}_i \in \{0,1\}^K$ where
$\sum_k y_{i,k} = 1$. A hard label $\vec{y}_i$ is fundamentally limited because it discards the underlying geometric similarity between classes; it identifies the correct class but says nothing about any other class, including ones that are visually or semantically close. A large teacher network\footnote{We will reference the teacher model with a superscript $T$ (e.g. $\hat{\vec{y}}^T$) and the student model with superscript $S$ (e.g. $\hat{\vec{y}}^S$).} trained to near-convergence absorbs this inter-class similarity into its weight matrices. Its output distribution $\hat{\vec{y}}^T$ places most mass on the correct class but assigns small but nonzero probabilities to visually similar classes, encoding what \citet{hinton2015distilling} call \emph{dark knowledge}. Knowledge distillation~\cite{hinton2015distilling} incorporates $\hat{\vec{y}}^T$ directly into the student's training objective alongside the ground-truth $\vec{y}_i$, so the student optimizes against both the correct answer and the teacher's learned similarity structure over all $K$ classes. This project studies that transfer — \emph{what} the teacher actually transfers, how it behaves under different hyperparameter settings, and where it fails.
%%%%%% A problem: when training with hard labels, the gradient of the CCE loss with respect to the pre-softmax logit $z_k^S$ at the output layer is simply $\hat{y}_k^S - y_k$. For a sample where the correct class has probability 0.95 and all others are near-zero, the gradient is dominated by a single signal. The small differences between near-zero probabilities — which carry the inter-class similarity information — contribute negligibly to the gradient.

\section{Literature Review}
\label{sec:litreview}
\subsection{Model Compression Landscape}
\label{sec:compression}

Based on the survey by \citet{gou2021knowledge}, modern model compression generally falls into three complementary families: knowledge \emph{distillation}, network \emph{pruning}, and network \emph{quantization}. Pruning removes redundant or inessential weights ($W_{ij}^{(l)} \approx 0$) (unstructured) or entire convolutional filters/weights (structured) from a pretrained network, reducing parameter count while leaving the architecture otherwise intact \citep{cheng2024surveydeepneuralnetwork}. Quantization reduces numerical precision of the weight matrices $W^{(l)}$ and activations $\vec{x}^{(l)}$---from 32-bit floats to 8-bit integers (INT8) or lower---shrinking memory and enabling faster arithmetic, also on the same architecture \citep{nagel2021whitepaperneuralnetwork}. Both methods start from a fixed network and modify it. Knowledge distillation takes a different approach entirely: it trains a new, compact (student) network from scratch, supervised by the larger (teacher) model. Pruning, quantization, and distillation are mutually compatible and are frequently pipelined in production environments. This paper is scoped to KD, and specifically to two forms of it: response-based distillation, which transfers knowledge through the teacher's output distribution, and feature-based distillation, which transfers it through intermediate layer representations. 

\subsection{Response-Based Distillation}
\label{sec:hinton}
\paragraph{The hard-label gradient problem.}
As established in Section~\ref{sec:intro}, the teacher's output $\hat{\vec{y}}^T$ encodes dark knowledge, the inter-class similarity structure that $\vec{y}_i$ discards. Can a student trained on hard labels access any of this structure? It cannot, and the reason is visible in the gradient.

The gradient of Eq.~\eqref{eq:cce} with respect to the student's pre-softmax logit $z_k^S$ is
\begin{equation}
  \frac{\partial L}{\partial z_k^S} = \hat{y}_k^S - y_k. % Note: Should proof be added to appendix?
  \label{eq:cce_grad}
\end{equation}
For every non-target class, $y_k = 0$, so the gradient reduces to $\hat{y}_k^S$. Because softmax outputs are strictly positive, this gradient is always positive. The gradient descent update becomes
$$ z_k^S \leftarrow z_k^S - \eta(\hat{y}_k^S) ,$$ 
strictly decreasing the logits of all non-target classes and pushing them toward $-\infty$, regardless of their true similarity to the correct class.

\paragraph{Temperature scaling.}
\citet{hinton2015distilling} solved this by introducing the temperature-scaled softmax.  For already-trained teacher logits $\vec{z}^T = [z_1^T, \dots, z_K^T]^T$, the soft output at temperature $\tau > 0$ is
\begin{equation}
\hat{y}_k^{T,\tau} = \frac{\exp(z_k^T / \tau)}{\sum_{j=1}^K \exp(z_j^T / \tau)}
\label{eq:softtarget}
\end{equation}
The same scaling is applied to the student to produce $\hat{y}_k^{S,\tau}$. Increasing $\tau$ softens the distribution by increasing its variance, thereby significantly increasing the relative numerical magnitude of the gradient of incorrect classes so that they generate meaningful gradients for the student network. At $\tau = 1$, Eq. \eqref{eq:softtarget} is a standard softmax. As $\tau \to \infty$, all scaled logits $z_k^T/\tau$ approach 0 and $e^0 = 1$, the distribution becomes asymptotically uniform ($\hat{y}_k^{T,\tau} \to 1/K$); conversely, as $\tau \to 0$, it collapses into a one-hot argmax function. Because distillation requires a smoother probability distribution, we typically operate where $\tau > 1$.

\paragraph{Training objective.}
The student is trained on a weighted sum of two losses, $L_{\mathrm{hard}}$ and $L_{\mathrm{soft}}$. The hard-label component is the standard CCE from Eq.~\eqref{eq:cce}, evaluated against $\vec{y}_i$ using the student's output at $\tau = 1$
\begin{equation}
  L_{\mathrm{hard}}
    = -\frac{1}{N}\sum_{i=1}^N\sum_{k=1}^K
        y_{i,k}\log\hat{y}_{i,k}^{S,1},
  \label{eq:lhard}
\end{equation}
ensuring that the student learns to predict the correct ground-truth labels. It anchors the student to the actual objective, preventing it from solely mimicking the teacher's potential errors or biases.
The soft-label component is the KL divergence between the softened-outputs of the teacher's and student's distributions, both evaluated at temperature $\tau>1$ via Eq.~\eqref{eq:softtarget}
\begin{align}
  L_{\mathrm{soft}}
    &= \frac{1}{N}\sum_{i=1}^N
        D_{\mathrm{KL}}\!\left(
          \hat{\vec{y}}_i^{T,\tau}
          \,\Big\|\,
          \hat{\vec{y}}_i^{S,\tau}
        \right) \nonumber \\
    &= \frac{1}{N}\sum_{i=1}^N\sum_{k=1}^K
        \hat{y}_{i,k}^{T,\tau}
        \log\frac{\hat{y}_{i,k}^{T,\tau}}{\hat{y}_{i,k}^{S,\tau}}\label{eq:lsoft}\\
    &= \frac{1}{N}\sum_{i=1}^N\sum_{k=1}^K
        \hat{y}_{i,k}^{T,\tau}
        \!\left(\log \hat{y}_{i,k}^{T,\tau} - \log \hat{y}_{i,k}^{S,\tau}\right)\nonumber\\
    &= \underbrace{\frac{1}{N}\sum_{i=1}^N\sum_{k=1}^K
        \hat{y}_{i,k}^{T,\tau}\log \hat{y}_{i,k}^{T,\tau}}_{-H(\hat{\vec{y}}^{T,\tau})}
      \;+\;
      \underbrace{\left(-\frac{1}{N}\sum_{i=1}^N\sum_{k=1}^K
        \hat{y}_{i,k}^{T,\tau}\log \hat{y}_{i,k}^{S,\tau}\right)}_{H(\hat{\vec{y}}^{T,\tau},\,\hat{\vec{y}}^{S,\tau})}\nonumber\\
    &= H(\hat{\vec{y}}^{T,\tau},\,\hat{\vec{y}}^{S,\tau}) - H(\hat{\vec{y}}^{T,\tau})\nonumber.
\end{align}
Since the teacher is fully pre-trained and its parameters are frozen during distillation, the teacher's output distribution $\hat{\vec{y}}^{T,\tau}$ is a constant with respect to the student parameters $\theta_S$. Consequently, $H(\hat{\vec{y}}^{T,\tau})$ is also a constant with respect to $\theta_S$, so minimizing $D_{\mathrm{KL}}(\hat{\vec{y}}^{T,\tau}\|\hat{\vec{y}}^{S,\tau})$ is equivalent to minimizing the cross-entropy $H(\hat{\vec{y}}^{T,\tau}, \hat{\vec{y}}^{S,\tau})$ with respect to the student's parameters.

\paragraph{The $\tau^2$ scaling factor.}
\citet{hinton2015distilling} argue that the soft-label loss in combined distillation loss must be weighted by $\tau^2$. To prove this to be the case, let us differentiate $L_{\mathrm{soft}}$, defined in Eq.~\eqref{eq:lsoft}, with respect to a student logit $z_k^S$. The gradient for a single sample (dropping index $i$ for convenience) is, by chain rule,
\begin{equation}
  \frac{\partial L_{\mathrm{soft}}}{\partial z_k^S}
  = \sum_{j=1}^{K}
      \frac{\partial L_{\mathrm{soft}}}{\partial \hat{y}_j^{S,\tau}}
      \cdot
      \frac{\partial \hat{y}_j^{S,\tau}}{\partial z_k^S}.
  \label{eq:chain}
\end{equation}
We evaluate each term individually. Since the teacher terms are
constant with respect to $\theta_S$, applying the logarithmic quotient rule and differentiating $-\sum_j \hat{y}_j^{T,\tau}\log\hat{y}_j^{S,\tau}$ with respect to $\hat{y}_j^{S,\tau}$ gives
\begin{equation}
  \frac{\partial L_{\mathrm{soft}}}{\partial \hat{y}_j^{S,\tau}}
  = -\frac{\hat{y}_j^{T,\tau}}{\hat{y}_j^{S,\tau}}.
  \label{eq:factor1}
\end{equation}
The student probability $\hat{y}_j^{S,\tau}$ depends on $z_k^S$
only through the scaled pre-activation $a_k^S = z_k^S/\tau$, so
a further application of the chain rule gives
\begin{equation}
  \frac{\partial \hat{y}_j^{S,\tau}}{\partial z_k^S}
  = \frac{\partial \hat{y}_j^{S,\tau}}{\partial a_k^S}
    \cdot
    \underbrace{\frac{\partial a_k^S}{\partial z_k^S}}_{=\,1/\tau},
  \label{eq:factor2_chain}
\end{equation}
where, by applying the quotient rule and considering the two distinct cases (diagonal and off-diagonal elements) for the jacobian,
\begin{equation}
  \frac{\partial \hat{y}_j^{S,\tau}}{\partial a_k^S}
  = \hat{y}_j^{S,\tau}\!\left(\delta_{jk} - \hat{y}_k^{S,\tau}\right).
  \label{eq:softmax_jac}
\end{equation}
Combining Eqs.~\eqref{eq:factor2_chain} and~\eqref{eq:softmax_jac} and substituting it along with Eq.~\eqref{eq:factor1} into Eq.~\eqref{eq:chain}
\begin{align}
  \frac{\partial L_{\mathrm{soft}}}{\partial z_k^S}
  &= \sum_{j=1}^{K}
      \left(-\frac{\hat{y}_j^{T,\tau}}{\hat{y}_j^{S,\tau}}\right)
      \cdot
      \frac{1}{\tau}\,\hat{y}_j^{S,\tau}
        \!\left(\delta_{jk} - \hat{y}_k^{S,\tau}\right)
  \nonumber\\
  &= \frac{1}{\tau}\sum_{j=1}^{K}
      \left(-\hat{y}_j^{T,\tau}\right)
        \!\left(\delta_{jk} - \hat{y}_k^{S,\tau}\right)
  \nonumber\\
  &= \frac{1}{\tau}\!\left[
      -\hat{y}_k^{T,\tau}
      + \hat{y}_k^{S,\tau}\underbrace{\sum_{j=1}^{K}
        \hat{y}_j^{T,\tau}}_{=\,1}
    \right]
  \nonumber\\
  &= \frac{1}{\tau}\!\left(\hat{y}_k^{S,\tau}
      - \hat{y}_k^{T,\tau}\right)
  \nonumber\\
  \intertext{Substituting Eq. \eqref{eq:softtarget}:}
  &= \frac{1}{\tau}\!\left(
        \frac{\exp(z_k^S/\tau)}{\sum_{j=1}^K \exp(z_j^S/\tau)}
        -
        \frac{\exp(z_k^T/\tau)}{\sum_{j=1}^K \exp(z_j^T/\tau)}
     \right)
  \nonumber\\
  \intertext{Applying first-order Taylor approximation $e^u \approx 1 + u$, valid when $\tau$ is large relative to the logit magnitudes \citep{hinton2015distilling}:}
  &\approx \frac{1}{\tau}\!\left(
        \frac{1 + z_k^S/\tau}{\sum_{j=1}^K (1 + z_j^S/\tau)}
        -
        \frac{1 + z_k^T/\tau}{\sum_{j=1}^K (1 + z_j^T/\tau)}
     \right)
  \nonumber\\
  &= \frac{1}{\tau}\!\left(
        \frac{1 + z_k^S/\tau}{K + \frac{1}{\tau}\sum_{j=1}^K z_j^S}
        -
        \frac{1 + z_k^T/\tau}{K + \frac{1}{\tau}\sum_{j=1}^K z_j^T}
     \right)
  \nonumber\\
  \intertext{Assuming logits are zero-meaned, i.e.\ $\sum_{j=1}^K z_j^S = \sum_{j=1}^K z_j^T = 0$ \citep{hinton2015distilling}:}
  &= \frac{1}{\tau}\!\left(
        \frac{1 + z_k^S/\tau}{K}
        -
        \frac{1 + z_k^T/\tau}{K}
     \right)
  \nonumber\\
  &= \frac{1}{K\tau}\!\left(
        1 + \frac{z_k^S}{\tau}
        - 1 - \frac{z_k^T}{\tau}
     \right)
  \nonumber\\
  &= \frac{1}{K\tau^2}\!\left(z_k^S - z_k^T\right).\nonumber
  \label{eq:soft_grad_final}
\end{align}
Therefore, the gradient of $L_{\mathrm{soft}}$ scales as $1/\tau^2$, relative to the gradient of $L_{\mathrm{hard}}$, which carries no dependence on $\tau$. If we simply add the soft loss and hard loss together, raising the temperature would cause the soft gradient to shrink proportionally to $1/\tau^2$. Without correction, the optimizer would effectively ignore the soft targets (the \emph{dark knowledge}) and revert almost entirely to standard hard-label training. Multiplying $L_{\mathrm{soft}}$ by $\tau^2$ exactly cancels this shrinkage, keeping the balance between the two loss terms stable regardless of the chosen $\tau$. The combined distillation objective is therefore:
%
\begin{equation}
  \boxed{
  L_{\mathrm{KD}}
    = \alpha \cdot L_{\mathrm{hard}}
    + (1-\alpha)\cdot\tau^2 \cdot L_{\mathrm{soft}}
  }
  \label{eq:lkd}
\end{equation}
where $\alpha \in [0,1]$ controls the relative weight of hard versus soft supervision and $\tau$ controls the softness of the teacher distribution via Eq.~\eqref{eq:softtarget}.

% \subsection{Feature-Based Distillation}
% \label{sec:fitnets}

\section{Methodology}
\label{sec:method}

\paragraph{Dataset.}
We conduct all experiments on CIFAR-100, a standard benchmark for multi-class image classification consisting of 60{,}000 colour images of resolution $32{\times}32$ pixels across $K=100$ classes, split into 50{,}000 training and 10{,}000 test images (500 train and 100 test images per class).

\paragraph{Teacher.}
We use ResNet-110 \citep{he2016deep} as the teacher network. Its depth and structure with skip connections allows it to learn rich inter-class similarity representations on CIFAR-100, making it a strong source of dark knowledge for distillation. The teacher is trained independently from scratch using standard cross-entropy against hard labels until convergence, and its weights are then frozen for all subsequent distillation experiments. We use a slightly longer schedule for the teacher than for the students (160 epochs with LR milestones at $\{80, 120\}$) to ensure the teacher converges to a higher accuracy ceiling and provides a stronger source of soft targets for distillation. All other hyperparameters match Table~\ref{tab:hyperparams}.

\paragraph{Student.}
We use ResNet-20 \citep{he2016deep} as the student network, which contains approximately 0.27 million parameters, a reduction of $6.3\times$ relative to the teacher. ResNet-20 is architecturally homogeneous with ResNet-110 (same residual blocks, same input resolution), which isolates depth as the sole variable differentiating teacher from student.

\subsection{Baseline: Standard Cross-Entropy Training}
\label{sec:baseline}
To establish a reference point, we train ResNet-20 independently from scratch on CIFAR-100 using the standard CCE loss of Eq.~\eqref{eq:cce} against one-hot hard labels, with no teacher supervision. The test accuracy of this baseline directly quantifies how much accuracy is lost by moving from the 110-layer teacher to the 20-layer student, providing the lower bound against which all distillation results are compared.

\paragraph{Student variants.}
To study the accuracy gap as a function of student capacity, we define two families of student networks. For the \textit{depth} study, we use ResNet-$d$ for $d \in \{20, 32, 44, 56\}$, all with base channel width 16. For the \textit{width} study, we use ResNet-20 with channel multiplier $w \in \{0.5, 1, 2\}$, corresponding to base channels $\{8, 16, 32\}$. The ResNet-20 with $w=1$ serves as the pivot point shared by both studies and is the architecture for which the baseline in Section~\ref{sec:baseline} was trained.
% \subsection{Response-Based Distillation}
% \label{sec:kd_hinton}
% % \subsection{Feature-Based Distillation}
% % \label{sec:kd_fitnets}
\subsection{Training Configuration}
\label{sec:training_config}
The baseline student is trained under the configuration given in Table~\ref{tab:hyperparams}. The same configuration will be reused for the teacher and the distilled student to ensure all comparisons are controlled.
\begin{table}[ht]
  \centering
  \caption{\textbf{Training configuration for the student baseline.} Teacher and distillation runs will follow the same configuration unless otherwise noted.\\}
  \label{tab:hyperparams}
  \begin{tabular}{lc}
    \toprule
    \textbf{Hyperparameter} & \textbf{Value} \\
    \midrule
    Model                & ResNet-20 (width 16) \\
    Optimiser            & SGD + Momentum \\
    Momentum             & 0.9 \\
    Initial LR           & 0.1 \\
    LR schedule          & MultiStep \\
    LR decay milestones  & epochs 60, 80 \\
    LR decay factor $\gamma$ & 0.1 \\
    Weight decay         & $5\times10^{-4}$ \\
    Batch size           & 128 \\
    Epochs               & 100 \\
    Loss                 & Cross-entropy \\
    \bottomrule
  \end{tabular}
\end{table}

% ========================================================================
\section{Results}
\label{sec:results}

\subsection{Baseline and Teacher}
\label{sec:results_baseline_teacher}

\paragraph{Baseline (ResNet-20, hard labels).}
Training ResNet-20 from scratch on CIFAR-100 under the configuration in Table~\ref{tab:hyperparams} with no teacher supervision establishes the lower bound that distillation must beat. Figure~\ref{fig:baseline} shows the Top-1 and Top-5 accuracy curves; the two sharp upward jumps at epochs 60 and 80 correspond to the LR decay milestones. The best-checkpoint metrics are summarised in Table~\ref{tab:baseline}. The $\approx$12.5\% point gap between Train Top-1 (80.11\%) and Test Top-1 (67.63\%) indicates mild overfitting.
\begin{figure}[ht]
  \centering
  \includegraphics[width=\columnwidth]{imgs/baseline training.jpeg}
  \caption{
    \textbf{Baseline ResNet-20 training curves on CIFAR-100.}
    Top-1 and Top-5 accuracy for train and test sets over 100 epochs.
    LR is decayed by $\gamma=0.1$ at epochs 60 and 80, producing the
    visible accuracy jumps.
  }
  \label{fig:baseline}
\end{figure}
\begin{table}[ht]
  \centering
  \caption{
    \textbf{Best-checkpoint metrics for the student baseline.} Best epoch determined by highest Test Top-1 accuracy.\\}
  \label{tab:baseline}
  \begin{tabular}{lc}
    \toprule
    \textbf{Metric} & \textbf{Value} \\
    \midrule
    Best Epoch          & 89      \\
    Test Top-1          & 67.63\% \\
    Test Top-5          & 91.31\% \\
    Train Top-1         & 80.11\% \\
    Train Top-5         & 96.74\% \\
    Train Loss          & 0.6754  \\
    Test Loss           & 1.1439  \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Teacher (ResNet-110).}
We train the ResNet-110 teacher independently with the longer schedule described in Section~\ref{sec:method}. Final-checkpoint metrics are summarised in Table~\ref{tab:teacher}. The teacher achieves \textbf{73.48\%} Test Top-1, providing the upper accuracy ceiling and the source of soft targets for all subsequent distillation experiments.
\begin{table}[ht]
  \centering
  \caption{\textbf{Best-checkpoint metrics for the teacher.} Trained on CIFAR-100 with hard-label cross-entropy for 160 epochs (LR milestones $\{80, 120\}$).\\}
  \label{tab:teacher}
  \begin{tabular}{lc}
    \toprule
    \textbf{Metric} & \textbf{Value} \\
    \midrule
    Architecture        & ResNet-110 \\
    Parameters          & 1{,}736{,}564 \\
    Best Epoch          & 129       \\
    Test Top-1          & 73.48\%   \\
    Test Top-5          & 92.74\%   \\
    \bottomrule
  \end{tabular}
\end{table}

The teacher--baseline gap of $73.48 - 67.63 = 5.85$ percentage points defines the headroom available to distillation: a perfect transfer of the teacher's knowledge would recover this entire gap, while no transfer would leave the student at the baseline.

\subsection{Hyperparameter Ablation}
\label{sec:results_hparam}

\paragraph{Temperature.}
Fixing the hard-label weight at $\alpha = 0.5$, we sweep the softmax temperature $\tau \in \{2, 4, 8, 16\}$. Higher $\tau$ produces flatter teacher distributions and exposes more inter-class similarity to the student, but at the cost of weaker discriminative signal. Results are reported in Table~\ref{tab:tau_sweep}.

\begin{table}[ht]
  \centering
  \caption{\textbf{Temperature ablation} ($\alpha=0.5$, ResNet-20 student, 100 epochs).}
  \label{tab:tau_sweep}
  \begin{tabular}{ccc}
    \toprule
    $\tau$ & Test Top-1 & $\Delta$ vs.\ baseline \\
    \midrule
    2  & 69.49\% & $+1.86$ \\
    4  & 69.64\% & $+2.01$ \\
    \textbf{8}  & \textbf{69.98\%} & $\mathbf{+2.35}$ \\
    16 & 69.73\% & $+2.10$ \\
    \bottomrule
  \end{tabular}
\end{table}

The temperature sweep produces a clean inverted-U with the peak at $\tau^* = 8$, recovering 40\% of the 5.85 pp teacher--baseline gap at zero parameter cost. The $\tau = 16$ cell drops back below $\tau = 8$, confirming that distributions that are too uniform lose the discriminative signal needed for the student to converge; conversely, $\tau = 2$ is too sharp and approaches the hard-label limit, partially defeating the purpose of soft supervision.

\paragraph{Loss weighting.}
Using the best temperature $\tau^*$ from above, we sweep $\alpha \in \{0.1, 0.5, 0.9\}$, where larger $\alpha$ places more weight on the hard-label cross-entropy term. Results are reported in Table~\ref{tab:alpha_sweep}.

\begin{table}[ht]
  \centering
  \caption{\textbf{Loss-weighting ablation} ($\tau = 8$, ResNet-20 student, 100 epochs).}
  \label{tab:alpha_sweep}
  \begin{tabular}{ccc}
    \toprule
    $\alpha$ & Test Top-1 & $\Delta$ vs.\ baseline \\
    \midrule
    0.1 & 69.74\% & $+2.11$ \\
    \textbf{0.5} & \textbf{69.98\%} & $\mathbf{+2.35}$ \\
    0.9 & 69.07\% & $+1.44$ \\
    \bottomrule
  \end{tabular}
\end{table}

The weighting sweep is qualitatively less sensitive than the temperature sweep: at the optimal $\tau=8$ the three $\alpha$ settings all improve on the baseline, but the balanced cell ($\alpha^* = 0.5$) remains the strongest. The deterioration at $\alpha = 0.9$ confirms that the soft-label term is doing real work---training with mostly hard labels and only a $10\%$ weight on the soft component recovers less than two-thirds of the gain available at the balanced setting. Conversely, $\alpha = 0.1$ (mostly soft) is competitive but does not exceed the balanced cell, suggesting the ground-truth signal continues to provide useful anchoring even when the teacher is doing most of the supervision.

\subsection{Architectural Scaling}
\label{sec:results_arch}

Using the optimal $(\tau^*, \alpha^*)$ identified above, we measure how the distillation benefit varies with student capacity along two axes.

\paragraph{Depth.}
Fixing width $w=1$ (base channels 16), we sweep ResNet-$d$ for $d \in \{20, 32, 44, 56\}$ ($n \in \{3, 5, 7, 9\}$ residual blocks per stage). Results are reported in Table~\ref{tab:depth_sweep}.

\begin{table}[ht]
  \centering
  \caption{\textbf{Depth ablation} ($\tau = \tau^*$, $\alpha = \alpha^*$, 100 epochs). \textit{[Numbers to be inserted.]}}
  \label{tab:depth_sweep}
  \begin{tabular}{lccc}
    \toprule
    Student & Params & Test Top-1 & $\Delta$ vs.\ baseline \\
    \midrule
    ResNet-20 & 278{,}324 & --.--\% & +--.-- \\
    ResNet-32 & 472{,}756 & --.--\% & +--.-- \\
    ResNet-44 & 667{,}188 & --.--\% & +--.-- \\
    ResNet-56 & 861{,}620 & --.--\% & +--.-- \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Width.}
Fixing depth at 20 layers, we sweep channel multiplier $w \in \{0.5, 1, 2\}$ (base channels 8, 16, 32). Results are reported in Table~\ref{tab:width_sweep}.

\begin{table}[ht]
  \centering
  \caption{\textbf{Width ablation} (ResNet-20, $\tau = \tau^*$, $\alpha = \alpha^*$, 100 epochs). \textit{[Numbers to be inserted.]}}
  \label{tab:width_sweep}
  \begin{tabular}{cccc}
    \toprule
    $w$ & Params & Test Top-1 & $\Delta$ vs.\ baseline \\
    \midrule
    0.5 &    71{,}756 & --.--\% & +--.-- \\
    1.0 &   278{,}324 & --.--\% & +--.-- \\
    2.0 & 1{,}096{,}196 & --.--\% & +--.-- \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[ht]
  \centering
  \includegraphics[width=\columnwidth]{../plots/analysis/capacity.png}
  \caption{
    \textbf{Distilled student accuracy as a function of student capacity ($\tau = 8$, $\alpha = 0.5$).}
    Depth axis (blue circles): ResNet-$d$ with $d \in \{20, 32, 44, 56\}$ at width $w = 1$. Width axis (red squares): ResNet-20 at $w \in \{0.5, 1, 2\}$. Both series share the ResNet-20 ($w=1$) pivot. Reference lines: hard-label ResNet-20 baseline (gray) and ResNet-110 teacher ceiling (black).
  }
  \label{fig:capacity}
\end{figure}

% ========================================================================
\section{Dark Knowledge Analysis}
\label{sec:dark_knowledge}
Figure~\ref{fig:dark_knowledge} shows, for six held-out CIFAR-100 test images, the teacher's top-5 softmax distribution at $T=1$ and $T=4$\footnote{Training used $\tau^* = 8$. For visualization we use $T = 4$, which gives a more legible bar plot while preserving the same qualitative structure (the top-$k$ ordering at $T=4$ and $T=8$ is identical).} alongside the predictions of the hard-label baseline student and the distilled student. At $T=1$ the teacher places nearly all probability mass on its argmax, identical in structure to a one-hot label; at $T=4$ the same logits produce a much flatter distribution that spreads tens of percentage points of mass onto semantically related classes. This off-argmax structure is the \emph{dark knowledge} \citep{hinton2015distilling} that the distillation objective transfers to the student: it tells the student not just \emph{which} class is correct but \emph{how} the teacher's learned representation organises the class manifold.

\begin{figure*}[ht]
  \centering
  \includegraphics[width=\textwidth]{../plots/analysis/dark_knowledge.png}
  \caption{
    \textbf{Teacher dark knowledge.} Each row is one held-out test image. Columns: input image; teacher softmax at $T{=}1$; teacher softmax at $T{=}4$; hard-label baseline student softmax at $T{=}1$; distilled student softmax at $T{=}1$. The temperature-softened teacher (red) reveals the inter-class similarity structure that the distillation loss exposes to the student.
  }
  \label{fig:dark_knowledge}
\end{figure*}

% ========================================================================
\section{Calibration Analysis}
\label{sec:calibration}
We additionally measure Expected Calibration Error (ECE) with 15 confidence bins on the CIFAR-100 test set. Table~\ref{tab:calibration} summarises results for the teacher, the hard-label baseline student, and the best distilled student (the $\tau=8$, $\alpha=0.5$ cell from Step 2a). Reliability diagrams are shown in Figure~\ref{fig:calibration}.

\begin{table}[ht]
  \centering
  \caption{\textbf{Calibration on CIFAR-100 test set} (ECE, 15 bins).}
  \label{tab:calibration}
  \begin{tabular}{lccc}
    \toprule
    Model & Top-1 & ECE & $\bar{p}_{\max}$ \\
    \midrule
    Teacher (ResNet-110)     & 73.48\% & 12.54\% & 0.860 \\
    Hard-label student       & 67.63\% & \textbf{6.23\%}  & 0.738 \\
    Distilled student ($\tau{=}8, \alpha{=}0.5$) & 69.98\% & 10.32\% & 0.803 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[ht]
  \centering
  \includegraphics[width=\columnwidth]{../plots/analysis/calibration.png}
  \caption{
    \textbf{Reliability diagrams (15 bins).} The hard-label baseline is the best-calibrated of the three; the distilled student inherits part of the teacher's overconfidence pattern.
  }
  \label{fig:calibration}
\end{figure}

Counter-intuitively, the \emph{hard-label} student is the best-calibrated model in the family. Despite achieving the highest Top-1 of the two students, the distilled student's mean max-probability shifts from $0.738$ (hard) toward the teacher's $0.860$ (final: $0.803$), and its ECE rises by $4.1$ percentage points (from $6.23\%$ to $10.32\%$). This finding---also reported by parallel groups working on the same project---is consistent with the view that the distillation objective transfers not just the teacher's class similarity structure but also its confidence calibration profile. Accuracy and calibration are therefore not jointly improved by KD: while KD raises accuracy by $+2.35$ pp, it actively degrades calibration. For applications where calibrated probabilities matter (e.g., abstention, downstream Bayesian fusion), an additional post-hoc calibration step (temperature scaling on a held-out set) would be required on top of the distilled model.

% ========================================================================
\section{Calibration Analysis}
\label{sec:calibration}
\textit{[Optional extension. Plan: report Expected Calibration Error (ECE) for teacher, hard-label baseline, and the best distilled student using 15 confidence bins, accompanied by reliability diagrams. Preliminary observation: the teacher is overconfident (ECE 12.54\%, mean max-prob 0.86), the hard-label student is comparatively well-calibrated (ECE 6.23\%, mean max-prob 0.74), and we will measure whether distillation inherits the teacher's overconfidence.]}

% ========================================================================
\section{Discussion}
\label{sec:discussion}

\paragraph{Temperature traces a clean inverted-U.}
The $\tau$ sweep in Table~\ref{tab:tau_sweep} traces the inverted-U trajectory that response-based KD theory predicts: at $\tau = 1$ the soft-label distribution collapses toward a one-hot vector and the soft component of $\mathcal{L}_{\text{KD}}$ becomes essentially a second hard-label term carrying no extra information; at $\tau \to \infty$ the softened distribution approaches the uniform distribution over $K$ classes and again carries no discriminative signal. The optimum at $\tau^* = 8$ in our experiments is somewhat higher than the canonical Hinton value of $\tau \approx 4$. We attribute this to two factors. First, our teacher (ResNet-110 trained for 160 epochs with multi-step decay) has substantially sharper output distributions than the ImageNet-style teachers Hinton et al.\ originally studied: at $\tau = 4$ the teacher still places $>40\%$ of mass on its argmax for typical CIFAR-100 examples, so further softening continues to yield useful information until $\tau \approx 8$. Second, with 100 classes the relative gradient magnitudes on non-target logits are inherently small, and a larger temperature amplifies them enough to provide a usable training signal in the early epochs.

\paragraph{$\alpha$ is less sensitive than $\tau$ but still optimal at the balanced setting.}
The $\alpha$ sweep in Table~\ref{tab:alpha_sweep} is qualitatively much flatter than the $\tau$ sweep --- all three cells improve over the baseline by between $+1.44$ and $+2.35$ pp --- but it still exhibits the same inverted-U structure with the peak at $\alpha^* = 0.5$. The deterioration at $\alpha = 0.9$ is informative: with $90\%$ weight on hard labels and only $10\%$ on the teacher, the student recovers $1.44$ pp of gain --- nontrivial, but only $\sim 60\%$ of what the balanced cell achieves. This rules out the possibility that the gain we observe is simply a regularization effect of the additional loss term; if it were, increasing the hard-label weight should not hurt. Conversely, $\alpha = 0.1$ remains competitive: the ground-truth labels continue to provide useful anchoring even when the teacher does most of the supervision, but they are not strictly necessary for the gain.

\paragraph{Capacity scaling.}
\textit{[To be written after Step 3 sweep completes. Plan: discuss how the depth and width sweeps in Tables~\ref{tab:depth_sweep}--\ref{tab:width_sweep} (visualized in Figure~\ref{fig:capacity}) test the capacity-gap hypothesis of \citet{cho2019efficacy}; whether the distilled accuracy approaches the teacher ceiling monotonically with student capacity; and whether depth and width are interchangeable axes of capacity for the purposes of KD.]}

\paragraph{Accuracy and calibration are not jointly improved.}
The most surprising finding of our study is summarised in Table~\ref{tab:calibration} and Figure~\ref{fig:calibration}: distillation makes the student measurably \emph{less} calibrated than the hard-label baseline despite making it measurably more accurate. The hard-label student has ECE $6.23\%$ and mean max-probability $0.738$; the distilled student has ECE $10.32\%$ and mean max-probability $0.803$. The student's confidence has shifted toward the teacher's confidence profile (the teacher sits at $0.860$). This is consistent with the view that the KL term in $\mathcal{L}_{\text{KD}}$ directly transfers the teacher's full output distribution --- including the calibration profile encoded in its temperature-scaled probabilities --- not just the argmax. The practical implication is that for downstream applications where calibration matters (selective prediction, abstention, downstream Bayesian fusion), KD alone is insufficient; an additional post-hoc calibration step on a held-out validation set would be required.

\paragraph{Limitations and threats to validity.}
\textit{(a) Single seed.} All cells were trained with a single random seed (42); the inverted-U pattern is robust enough that this is unlikely to invert, but the gap of $0.49$ percentage points between $\tau = 2$ and $\tau = 8$ is close to the per-seed run-to-run variance reported in the KD literature, so the precise location of $\tau^*$ should be read with caution. \textit{(b) Teacher schedule.} Our teacher used a slightly longer schedule than the students (Section~\ref{sec:method}). The trained teacher's $73.48\%$ Top-1 sits below the typical literature ceiling for ResNet-110 on CIFAR-100 ($\sim 76\%$), so the absolute distilled numbers reported here are lower than what a fully-converged teacher would yield; the relative shape of the ablation surfaces should generalise. \textit{(c) Same-architecture, response-only.} We study one architecture family (ResNet-CIFAR) and one form of distillation (response-based, Hinton-style). The mid-report committed to a same-architecture, response-only study; cross-architecture distillation and feature-based methods \citep{romero2015fitnets, zhao2022decoupled} are out of scope here.

% ========================================================================
\section{Conclusion}
\label{sec:conclusion}

We studied response-based knowledge distillation \citep{hinton2015distilling} as a model-compression strategy for deploying compact CIFAR-100 image classifiers on resource-constrained devices. A pretrained ResNet-110 teacher ($73.48\%$ Top-1) supervised a $6.3\times$ smaller ResNet-20 student via the combined hard-label cross-entropy + temperature-scaled KL divergence objective. Our principal findings:

\begin{enumerate}
\item \textbf{Distillation closes $40\%$ of the teacher--baseline gap at zero parameter cost.} The best distilled ResNet-20 ($\tau^* = 8$, $\alpha^* = 0.5$) reaches $69.98\%$ Top-1, recovering $2.35$ pp of the $5.85$ pp gap between the hard-label baseline ($67.63\%$) and the teacher ceiling ($73.48\%$). Distillation is a strictly Pareto-improving step on the same student architecture.

\item \textbf{Temperature traces an inverted-U with peak at $\tau^* = 8$.} The $\tau \in \{2, 4, 8, 16\}$ sweep produces a monotonic rise from $69.49\%$ at $\tau = 2$ to $69.98\%$ at $\tau = 8$, followed by a drop back to $69.73\%$ at $\tau = 16$. This empirically validates the theoretical prediction that distributions which are either too sharp (collapsing to hard labels) or too flat (collapsing to uniform) lose the discriminative signal needed for the student to converge.

\item \textbf{Loss weighting is less sensitive but still favours a balanced setting.} All three $\alpha$ values ($0.1, 0.5, 0.9$) improve over the baseline, but $\alpha = 0.5$ is optimal. The $\alpha = 0.9$ cell (mostly hard supervision) recovers only $62\%$ of the gain at $\alpha = 0.5$, ruling out a pure-regularisation explanation for the distillation effect.

\item \textbf{Capacity-scaling.} \textit{[To be completed with Step 3 numbers. Plan: state whether distilled accuracy increases monotonically with student depth and width, whether the wider ResNet-20 ($w=2$) or the deeper ResNet-56 is the more parameter-efficient route to teacher-level accuracy, and what the narrow $w=0.5$ student's performance reveals about the minimum capacity needed to absorb dark knowledge.]}

\item \textbf{Distillation degrades calibration even as it improves accuracy.} The distilled student's ECE rises from $6.23\%$ (hard baseline) to $10.32\%$, with mean max-probability shifting from $0.738$ toward the teacher's $0.860$. The KL term in the distillation objective transfers the teacher's full output distribution---including its confidence calibration profile---not merely its argmax. For applications where calibration matters, KD must be paired with a post-hoc calibration step.

\item \textbf{Dark knowledge is directly observable.} Per-image bar plots of teacher softmax at $T = 1$ vs $T = 4$ make the inter-class similarity structure visually explicit. The distilled student's output distributions are visibly closer to the temperature-softened teacher than to the hard-label baseline, providing a qualitative mechanism for the accuracy gain reported in (1).
\end{enumerate}

Future extensions of this study include cross-architecture distillation (transferring from a CNN teacher to architecturally distinct mobile-friendly students such as MobileNetV2), feature-level methods that distill intermediate representations alongside output logits \citep{romero2015fitnets}, and combining distillation with complementary compression strategies (structured pruning, INT8 quantization).

% ========================================================================
\section*{Statement on the Use of AI Tools}
AI coding assistants (Claude Code) were used by the team for software scaffolding (organising the trainer / data / model module structure under the existing codebase conventions), debugging environmental issues (Python SSL handling on macOS, MPS device selection), generating boilerplate plotting code, and producing structural drafts of this report. All experimental design --- choice of ablation grid, choice of architectures, choice of evaluation metrics --- as well as all interpretation of results were made by the team. The mathematical derivations in Section~\ref{sec:method} (in particular the $\tau^2$ scaling factor proof) were written and verified by the team without AI assistance. All trained checkpoints were produced by the team's own training runs; no pretrained weights were imported from external sources.

\bibliography{references}
\bibliographystyle{icml2025}

\end{document}
